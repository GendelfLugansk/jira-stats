<div class="uk-card uk-card-default uk-card-small">
  <div class="uk-card-header">
    <div class="uk-grid uk-grid-small">
      <div class="uk-width-auto"><h4>Histogram of Work Ratio (Logged Time / Estimated Time)</h4></div>
    </div>
  </div>
  <div class="uk-card-body">
    {{#if plotlyData.length}}
      <div class="chart-container" id="{{chartId}}"></div>
    {{else}}
      <p id="{{chartId}}">
        It seems that you don't have enough issues to build this chart. Maybe you want to {{link-to 'Import' 'import'}}?
      </p>
    {{/if}}
    <p class="uk-text-meta">
      Shows percentage of issues by work ratio. Only issues with status 'Done' included.
    </p>
  </div>
</div>